[tools]
# Runtimes
node = "lts"
python = "latest"
go = "latest"

# Python tooling
uv = "latest"

# Utilities (from mise registry)
ripgrep = "latest"
fd = "latest"
fzf = "latest"
jq = "latest"
bat = "latest"
delta = "latest"

# AI CLIs (via aqua backend)
"aqua:anthropics/claude-code" = "latest"

[env]
# User can add custom env vars here
# Example:
# MY_API_KEY = "{{ env.MY_API_KEY }}"
